package apincer.mobile.tradings.ui.theme

import androidx.compose.ui.graphics.Color

// Midnight Blue Palette (Dark Theme)
val NightBackground = Color(0xFF0F172A) // Midnight Blue (softer than pitch black)
val NightSurface = Color(0xFF1E293B)    // Slate surface
val NightPrimary = Color(0xFF7DD3FC)    // Soft Sky Blue
val NightSecondary = Color(0xFFC4B5FD)  // Soft Lavender
val NightTertiary = Color(0xFF6EE7B7)   // Soft Mint

val NightSuccess = Color(0xFF6EE7B7)    // Soft Mint (Profit)
val NightError = Color(0xFFFCA5A5)      // Soft Coral (Loss)
val NightWarning = Color(0xFFFCD34D)    // Soft Amber (Potential)

// Glass Effects
val GlassBorder = Color(0xFFFFFFFF).copy(alpha = 0.12f)
val DeepNavy = Color(0xFF1B263B)
val RichBlue = Color(0xFF0D1B2A)
val GoldAccent = Color(0xFFC5A059)
val GrowthGreen = Color(0xFF2D6A4F)
val LossRed = Color(0xFFBC4749)

// Text Colors
val OffWhite = Color(0xFFF1F5F9)
val MutedNavy = Color(0xFF64748B)
val SoftGold = Color(0xFFE0C9A6)
val SoftBlue = Color(0xFFBAE6FD)

// Base Colors
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val Gray = Color(0xFF6C757D)
val LightGray = Color(0xFFE9ECEF)
